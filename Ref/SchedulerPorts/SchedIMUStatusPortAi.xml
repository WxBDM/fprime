<!-- If the IMU is enabled or not (FROM scheduler TO IMU Manager) -->
<interface name="SchedIMUPhase" namespace="Ref">
    <args>
        <arg name="status_imu" type="U32">
		</arg>
    </args>
</interface>
